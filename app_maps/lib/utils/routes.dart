class MyRoutes {
  // we dont need to make objects again and again so we make it static;
  static String loginRoute = "/login";
  static String homeRoute = "/home";
  static String googleMaps = "/googleMaps";
}
